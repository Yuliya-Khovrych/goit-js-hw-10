{{#each this}}
  <li class='country-list__item'>
    <img
      class='country-list__flag'
      src='{{flags.svg}}'
      alt='Flag of {{name.official}}'
      width='50px'
      height='auto'
    />
    <h2>{{name.official}}</h2>
  </li>
{{/each}}